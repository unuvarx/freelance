import React, {useState} from "react";


export default function Alert({visibility}) {
  const [clickX, setClickX] = useState(false)
  const [offButton, setoffButton] = useState(second)
  return (
    <div></div>
  );
}
